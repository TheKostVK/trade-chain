/**
 * Заглушка для импортов вида `icon.svg?react` в тестах.
 *
 * Разбором SVG в компонент занимается плагин сборки, и в среде тестов он
 * недоступен: любой модуль, который тянет за собой иконку, падал на импорте
 * целиком — вместе с чистыми функциями, ради которых тест и писали.
 * Внешний вид иконки в юнит-тестах не проверяется, поэтому подменяется пустым
 * элементом с тем же интерфейсом.
 */
import type { SVGProps } from 'react';

const SvgStub = (props: SVGProps<SVGSVGElement>) => <svg {...props} />;

export default SvgStub;
